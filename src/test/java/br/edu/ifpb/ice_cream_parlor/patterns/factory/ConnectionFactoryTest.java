package br.edu.ifpb.ice_cream_parlor.patterns.factory;

import org.junit.jupiter.api.Test;

import java.sql.Connection;

import static org.junit.jupiter.api.Assertions.*;

public class ConnectionFactoryTest {

    @Test
    void testCreateConnection() {
//        try (Connection connection = PostgresConnectionFactory.createConnection()) {
//            assertNotNull(connection, "Conexão não deve ser nula");
//            assertFalse(connection.isClosed(), "Conexão não deve estar fechada");
//        } catch (Exception e) {
//            fail("Falha ao abrir conexão: " + e.getMessage());
//        }
    }
}
